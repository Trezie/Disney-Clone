/*export const signIn = () => {
  signInWithPopup(auth, provider)
  .then((result)=>{
    console.log(result);
  })
}*/

const signIn = () => {
  auth.signInWithPopup(provider)
  .then((result)=>{
    console.log(result)
  })
}

header

const signin = () => {
    auth.signInWithPopup(provider)
    .then((result)=>{
      console.log(result)
    })
  }  